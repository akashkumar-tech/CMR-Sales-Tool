# frontend/.env (committed) points at the hosted preview backend.
# For local development, copy this file to frontend/.env.local (git-ignored; overrides .env):
REACT_APP_BACKEND_URL=http://localhost:8001
